Job {
  Name = "backup-copysrc"
  JobDefs = "DefaultJob"
  MaximumConcurrentJobs = 10

  # A backup into a pool of its own, for the copy case. Full Backup Pool
  # too: DefaultJob sets it to Full, which would win over Pool.
  Storage = FileB
  Pool = CopySrc
  Full Backup Pool = CopySrc
}
