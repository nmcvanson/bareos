Job {
  Name = "backup-shared-reversed"
  JobDefs = "DefaultJob"
  MaximumConcurrentJobs = 10

  # The same two members in the opposite order, so alternating runs of this
  # and backup-shared land on alternating storages.
  Storage = FileD, FileC
}
