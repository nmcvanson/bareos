# The Pool names a policy that does not exist. Parsing must fail.

Director {
  Name = "bareos-dir"
  Password = "secret"
}

Storage {
  Name = "storage01"
  Address = localhost
  Password = "secret"
  Device = "dev1"
  MediaType = "Type"
}

Pool {
  Name = "pool-bad-policy"
  Pool Type = Backup
  Storage = storage01
  Storage Group Policy = ThisPolicyDoesNotExist
}
